/** How long after sending a user may still delete their own direct message. */
export const DM_DELETE_WINDOW_MS = 30 * 60 * 1000
/** How long after sending a user may still edit their own direct message. */
export const DM_EDIT_WINDOW_MS = 15 * 60 * 1000
